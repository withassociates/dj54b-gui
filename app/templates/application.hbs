<h2 id='title'>Welcome to DJ54B</h2>

<p>
  {{#if isPlaying}}
    <button {{action "pause"}}>Pause</button>
  {{else}}
    <button {{action "play"}}>Play</button>
  {{/if}}
  <button {{action "next"}}>Next</button>
  <button {{action "up"}}>Up</button>
  <button {{action "down"}}>Down</button>
</p>
<p>Track: {{track.name}} by {{track.artist}}</p>
<p>State: {{state}}</p>
<p>Volume: {{volume}}</p>
<p>{{spotify-art trackID=track.id}}</p>
